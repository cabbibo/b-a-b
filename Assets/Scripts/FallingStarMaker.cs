using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FallingStarMaker : MonoBehaviour
{

    // At random times, spawn afalling star
    // play a specific sound, take over camera view for a moment ( tween in and out for that strength )
    //time out for when it turns off, still has something pointed to the sky so we can see where it is
    // when it times out some sort of visual representation

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
